_base_: base.yaml

model:
  type: c3d
  pretrained: true

data:
  clip_len: 16
  img_size: 112
  sampling: uniform

train:
  lr: 0.01
  warmup_epochs: 5
  warmup_start_factor: 0.1
  max_epochs: 100

logging:
  wandb_name: c3d_baseline

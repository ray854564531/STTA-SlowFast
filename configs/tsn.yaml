_base_: base.yaml

model:
  type: tsn
  pretrained: true

data:
  clip_len: 8
  img_size: 224
  sampling: segment
  segment_window: 64

train:
  lr: 0.01
  warmup_epochs: 5
  warmup_start_factor: 0.1
  max_epochs: 100

logging:
  wandb_name: tsn_baseline

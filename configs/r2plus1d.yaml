_base_: base.yaml

model:
  type: r2plus1d
  pretrained: true

data:
  clip_len: 8
  img_size: 224  # must be 224: r2plus1d_r50 uses AvgPool3d(4,7,7) spatial kernel
  sampling: uniform

logging:
  wandb_name: r2plus1d

_base_: base.yaml

model:
  type: i3d
  pretrained: true

data:
  clip_len: 32
  img_size: 224
  sampling: uniform

train:
  lr: 0.01
  warmup_epochs: 5
  warmup_start_factor: 0.1
  max_epochs: 100

logging:
  wandb_name: i3d_baseline
